id: init_realm
author: Default
changes:
  - addRealm:
      name: local_realm

  - updateRealm:
      id: local_realm
      unmanagedAttributePolicy: "ADMIN_EDIT"

  - addClientScope:
      realm: local_realm
      name: openid
