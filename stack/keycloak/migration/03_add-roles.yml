id: add roles
author: Default
realm: local_realm
changes:
  - addRole:
      name: anwender
      description: Simple user role to access the application
      clientRole: true
      clientId: local
      composite: false

  - addRole:
      name: fachadmin
      description: Admin role to access the admin page and change settings
      clientRole: true
      clientId: local
      composite: false
